# FX Revenue Dashboard

A small dashboard that shows FX revenue per trade.

## Running the tests
```
python -m unittest discover -s tests
```

## Viewing the dashboard
Serve the `web/` folder with any static server, e.g. `python -m http.server -d web`.
